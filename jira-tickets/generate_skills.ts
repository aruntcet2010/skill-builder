/**
 * Generate Claude Code skills from historical Jira tickets.
 * Pure TypeScript orchestrator — all LLM work delegated to focused agents.
 *
 * Usage:
 *   npx tsx jira-tickets/generate_skills.ts
 */

import { OrchestratorTracer } from "../lib/tracer.js";

// TODO: implement Jira skill generation pipeline
